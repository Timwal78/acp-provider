services:
  # ── Worker: ACP event listener (job intake) ─────────────────────────────
  - type: worker
    name: acp-provider-scriptmasterlabs
    runtime: docker
    plan: starter
    dockerfilePath: ./Dockerfile
    autoDeploy: true
    envVars:
      - key: ACP_CONFIG_JSON
        sync: false
      - key: ACP_SIGNER_KEYS_JSON
        sync: false
      - key: ACP_KEYRING_KEY_B64
        sync: false
      - key: ACP_AGENT_WALLET_ADDRESS
        value: "0x72330994f379a71542e7bd5a4cf99a9d9743f4aa"
      - key: ACP_REFRESH_TOKEN
        sync: false
      - key: PYTHONUNBUFFERED
        value: "1"
      - key: FRED_API_KEY
        sync: false
      - key: CONGRESS_API_KEY
        sync: false
      - key: SAM_API_KEY
        sync: false

  # ── Web service: x402/A2A/MCP HTTP layer ────────────────────────────────
  # Same Docker image, but overrides CMD to run gunicorn instead of provider.py.
  # This binds to $PORT so Render detects it as a live web service.
  - type: web
    name: acp-x402-scriptmasterlabs
    runtime: docker
    plan: starter
    dockerfilePath: ./Dockerfile
    autoDeploy: true
    dockerCommand: python3 -m gunicorn x402_server:app --bind 0.0.0.0:$$PORT --workers 2 --timeout 120
    # Beacon attestations and settlement records are the reputation ledger.
    # They defaulted to /tmp, which Render wipes on every deploy and restart,
    # so the beacon could never leave cold_start_prior no matter how many real
    # payments cleared. A persistent disk makes that history durable.
    disk:
      name: beacon-data
      mountPath: /var/data
      sizeGB: 1
    envVars:
      - key: PYTHONUNBUFFERED
        value: "1"
      - key: BEACON_STORE
        value: "/var/data/sml_beacon_store.json"
      - key: X402_PAY_TO
        value: "0x72330994f379a71542e7bd5a4cf99a9d9743f4aa"
      - key: X402_NETWORK
        value: "base"
      - key: CDP_API_KEY_ID
        sync: false
      - key: CDP_API_KEY_SECRET
        sync: false
      - key: OPERATOR_API_KEY
        sync: false
      - key: SAM_API_KEY
        sync: false
